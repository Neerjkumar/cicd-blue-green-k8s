apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: production
spec:
  selector:
    app: myapp
    version: blue  # gets switched during deployment to green
  ports:
  - port: 80
    targetPort: 8080
  type: LoadBalancer
